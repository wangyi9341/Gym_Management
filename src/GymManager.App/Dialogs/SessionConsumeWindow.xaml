<Window x:Class="GymManager.App.Dialogs.SessionConsumeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="课程消耗"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize">

    <materialDesign:Card Padding="16" Margin="16" MinWidth="520">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                       Text="新增消课记录"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Margin="0,0,0,12" />

            <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="消课次数" />
            <TextBox Grid.Row="1" Grid.Column="1"
                     Text="{Binding SessionsUsed, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                     materialDesign:HintAssist.Hint="例如：1" />

            <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="时间" Margin="0,12,0,0" />
            <DatePicker Grid.Row="2" Grid.Column="1"
                        Margin="0,12,0,0"
                        SelectedDate="{Binding UsedAt, Mode=TwoWay}" />

            <TextBlock Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Text="备注" Margin="0,12,0,0" />
            <TextBox Grid.Row="3" Grid.Column="1"
                     Margin="0,12,0,0"
                     Text="{Binding Note, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                     materialDesign:HintAssist.Hint="可选" />

            <StackPanel Grid.Row="4" Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,16,0,0">
                <Button Content="取消"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Margin="0,0,8,0" />
                <Button Content="保存"
                        Command="{Binding SaveCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}" />
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</Window>
