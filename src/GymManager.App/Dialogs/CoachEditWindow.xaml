<Window x:Class="GymManager.App.Dialogs.CoachEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="教练信息"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize">

    <materialDesign:Card Padding="16" Margin="16" MinWidth="420">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                       Text="教练信息"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Margin="0,0,0,12" />

            <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="工号" />
            <TextBox Grid.Row="1" Grid.Column="1"
                     Text="{Binding EmployeeNo, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                     IsReadOnly="{Binding IsEmployeeNoReadOnly}"
                     materialDesign:HintAssist.Hint="请输入工号" />

            <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="姓名" Margin="0,12,0,0" />
            <TextBox Grid.Row="2" Grid.Column="1"
                     Margin="0,12,0,0"
                     Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                     materialDesign:HintAssist.Hint="请输入姓名" />

            <StackPanel Grid.Row="3" Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,16,0,0">
                <Button Content="取消"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Margin="0,0,8,0" />
                <Button Content="保存"
                        Command="{Binding SaveCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}" />
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</Window>

