<Window x:Class="GymManager.App.Dialogs.PrivateTrainingMemberEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="私教课会员"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize">

    <materialDesign:Card Padding="16" Margin="16" MinWidth="520">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                       Text="私教课会员信息"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Margin="0,0,0,12" />

            <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="姓名" />
            <TextBox Grid.Row="1" Grid.Column="1"
                     Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                     materialDesign:HintAssist.Hint="请输入会员姓名" />

            <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="性别" Margin="0,12,0,0" />
            <ComboBox Grid.Row="2" Grid.Column="1"
                      Margin="0,12,0,0"
                      ItemsSource="{Binding GenderOptions}"
                      SelectedItem="{Binding Gender, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource GenderToText}}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <TextBlock Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Text="电话号" Margin="0,12,0,0" />
            <TextBox Grid.Row="3" Grid.Column="1"
                     Margin="0,12,0,0"
                     Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                     materialDesign:HintAssist.Hint="请输入电话号" />

            <TextBlock Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Text="总课程数" Margin="0,12,0,0" />
            <TextBox Grid.Row="4" Grid.Column="1"
                     Margin="0,12,0,0"
                     Text="{Binding TotalSessions, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                     materialDesign:HintAssist.Hint="例如：30" />

            <TextBlock Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" Text="初始缴费" Margin="0,12,0,0" />
            <TextBox Grid.Row="5" Grid.Column="1"
                     Margin="0,12,0,0"
                     Text="{Binding InitialPaidAmount, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                     IsReadOnly="{Binding IsEditMode}"
                     materialDesign:HintAssist.Hint="仅新增时可填写（编辑请走“费用记录”）" />

            <StackPanel Grid.Row="6" Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,16,0,0">
                <Button Content="取消"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Margin="0,0,8,0" />
                <Button Content="保存"
                        Command="{Binding SaveCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}" />
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</Window>
