<Window x:Class="GymManager.App.Dialogs.AnnualCardPauseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="停卡"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize">

    <materialDesign:Card Padding="16" Margin="16" MinWidth="520">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                       Text="年卡停卡"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Margin="0,0,0,12" />

            <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="会员" />
            <TextBlock Grid.Row="1" Grid.Column="1"
                       Text="{Binding MemberName}"
                       VerticalAlignment="Center"
                       FontWeight="SemiBold" />

            <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="停卡天数" Margin="0,12,0,0" />
            <TextBox Grid.Row="2" Grid.Column="1"
                     Margin="0,12,0,0"
                     Text="{Binding PauseDays, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                     materialDesign:HintAssist.Hint="例如：7" />

            <TextBlock Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Text="截止日期" Margin="0,12,0,0" />
            <StackPanel Grid.Row="3" Grid.Column="1" Margin="0,12,0,0">
                <TextBlock>
                    <Run Text="{Binding CurrentEndDate, Mode=OneWay, StringFormat={}{0:yyyy-MM-dd}}" />
                    <Run Text="  →  " />
                    <Run Text="{Binding NewEndDate, Mode=OneWay, StringFormat={}{0:yyyy-MM-dd}}" />
                </TextBlock>
                <TextBlock Text="规则：停卡按约定天数生效，到期后自动恢复。"
                           Opacity="0.70"
                           Margin="0,6,0,0" />
            </StackPanel>

            <StackPanel Grid.Row="4" Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,16,0,0">
                <Button Content="取消"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Margin="0,0,8,0" />
                <Button Content="确定"
                        Command="{Binding SaveCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}" />
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</Window>
