<UserControl x:Class="GymManager.App.Views.PrivateTrainingMembersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="私教课会员管理"
                       FontSize="22"
                       FontWeight="SemiBold"
                       DockPanel.Dock="Left" />

            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="刷新"
                        Command="{Binding RefreshCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Margin="0,0,8,0" />
                <Button Content="新增"
                        Command="{Binding AddCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="0,0,8,0" />
                <Button Content="编辑"
                        Command="{Binding EditCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Margin="0,0,8,0" />
                <Button Content="删除"
                        Command="{Binding DeleteCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Margin="0,0,16,0" />

                <Button Content="新增缴费"
                        Command="{Binding AddFeeCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Margin="0,0,8,0" />
                <Button Content="消课"
                        Command="{Binding ConsumeSessionsCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}" />
            </StackPanel>
        </DockPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox Width="260"
                     Text="{Binding Keyword, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="姓名/电话搜索" />
            <Button Content="搜索"
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Margin="8,0,0,0" />
            <ProgressBar Width="160"
                         Height="4"
                         Margin="16,14,0,0"
                         IsIndeterminate="True"
                         Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" />
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left: Member list -->
            <materialDesign:Card Grid.Column="0" Padding="8" Margin="0,0,12,0">
                <DataGrid ItemsSource="{Binding Members}"
                          SelectedItem="{Binding SelectedMember, Mode=TwoWay}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="姓名" Binding="{Binding Name}" Width="120" />
                        <DataGridTextColumn Header="性别" Binding="{Binding Gender, Converter={StaticResource GenderToText}}" Width="70" />
                        <DataGridTextColumn Header="电话" Binding="{Binding Phone}" Width="140" />
                        <DataGridTextColumn Header="已交费用" Binding="{Binding PaidAmount}" Width="110" />
                        <DataGridTextColumn Header="总课程" Binding="{Binding TotalSessions}" Width="90" />
                        <DataGridTextColumn Header="已用" Binding="{Binding UsedSessions}" Width="70" />
                        <DataGridTextColumn Header="剩余" Binding="{Binding RemainingSessions}" Width="70" />
                        <DataGridTextColumn Header="更新时间" Binding="{Binding UpdatedAt, StringFormat={}{0:MM-dd HH:mm}}" Width="110" />
                    </DataGrid.Columns>
                </DataGrid>
            </materialDesign:Card>

            <!-- Right: Details -->
            <StackPanel Grid.Column="1">
                <materialDesign:Card Padding="12" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="会员详情" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <TextBlock Text="{Binding SelectedMember.Name, TargetNullValue=未选择会员}" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding SelectedMember.Phone}" Opacity="0.8" />

                        <WrapPanel Margin="0,12,0,0">
                            <materialDesign:Chip Content="{Binding SelectedMember.PaidAmount, StringFormat={}{0} 元}" Margin="0,0,8,8" />
                            <materialDesign:Chip Content="{Binding SelectedMember.TotalSessions, StringFormat=总课程：{0}}" Margin="0,0,8,8" />
                            <materialDesign:Chip Content="{Binding SelectedMember.UsedSessions, StringFormat=已用：{0}}" Margin="0,0,8,8" />
                            <materialDesign:Chip Content="{Binding SelectedMember.RemainingSessions, StringFormat=剩余：{0}}" Margin="0,0,8,8" />
                        </WrapPanel>
                    </StackPanel>
                </materialDesign:Card>

                <TabControl>
                    <TabItem Header="费用记录">
                        <materialDesign:Card Padding="8" Margin="0,8,0,0">
                            <DataGrid ItemsSource="{Binding FeeRecords}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="时间" Binding="{Binding PaidAt, StringFormat={}{0:yyyy-MM-dd}}" Width="140" />
                                    <DataGridTextColumn Header="金额" Binding="{Binding Amount}" Width="100" />
                                    <DataGridTextColumn Header="备注" Binding="{Binding Note}" Width="*" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </materialDesign:Card>
                    </TabItem>
                    <TabItem Header="消课记录">
                        <materialDesign:Card Padding="8" Margin="0,8,0,0">
                            <DataGrid ItemsSource="{Binding SessionRecords}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="时间" Binding="{Binding UsedAt, StringFormat={}{0:yyyy-MM-dd}}" Width="140" />
                                    <DataGridTextColumn Header="消耗" Binding="{Binding SessionsUsed}" Width="80" />
                                    <DataGridTextColumn Header="备注" Binding="{Binding Note}" Width="*" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </materialDesign:Card>
                    </TabItem>
                </TabControl>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
